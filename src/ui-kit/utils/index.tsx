import {
  formatAddress,
  formatAmount,
  formatHumanAmount,
  maxDecimals
} from "./format"
import slug from "./slug"

const utils = {
  formatAddress,
  formatAmount,
  formatHumanAmount,
  maxDecimals,
  slug
}

export default utils