import {
  formatAddress,
  formatAmount,
  formatHumanAmount
} from "./format"
import slug from "./slug"

const utils = {
  formatAddress,
  formatAmount,
  formatHumanAmount,
  slug
}

export default utils